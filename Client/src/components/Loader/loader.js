import React from 'react';
import './style.scss';

const Loader = (props) => {
    return (
        <div className="loader-body">
            <div className="loader"></div>
        </div>
    )
}

export default Loader;